# Training dependencies for Source Tiering LoRA fine-tuning
# Install: pip install -r packages/training/requirements-training.txt
#
# Note: unsloth requires CUDA. On Windows with RTX 5060:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
#   pip install --no-deps trl peft accelerate bitsandbytes
#
# Alternative if unsloth install fails on Windows:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install transformers peft trl datasets accelerate bitsandbytes

unsloth
trl>=0.8.0
datasets>=2.18.0
accelerate>=0.28.0
bitsandbytes>=0.43.0
