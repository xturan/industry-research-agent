# Risks

- Existing dirty worktree prevents clean git-based scope proof.
- `.agent` is untracked, so git cannot isolate Phase-level changes cleanly.
- Future skill proliferation could make routine tasks heavy if trigger rules are not kept narrow.
- Future edits to `AGENTS.md` should be handled under an explicit PLAN phase with validation.
