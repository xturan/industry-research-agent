---
name: systematic-debugging
description: Use when a command, test, API call, integration, live eval, provider request, or user-reported behavior fails unexpectedly, flakes, times out, or produces surprising output.
---

# Systematic Debugging

Use this native wrapper to surface the debugging gate before patching around failures.

Authoritative source: .agent/skills/systematic-debugging.md

Follow the authoritative `.agent` skill and the active PLAN. Reproduce the failure, inspect real output, identify the failure class, and only then implement the smallest safe fix.

Do not duplicate or reinterpret the full rule body here.
